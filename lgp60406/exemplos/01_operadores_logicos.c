/* Programa básico que exibe "olá mundo" */

/* Bibliotecas */
#include <stdio.h>


/* Função main: ponto de entrada */
int main() {
	/* Variáveis */
	int r=-1;
	int s=1;
	int t=2;

	/* Simbolos de conversao
	Simbolo
	    !         negação
	   &&         and
	   ||         or */

	/* Operadores
	 >    maior
	 <    menor
	 <=   menor igual
	 >=   maior igual
	 ==   igual
	 !=   diferente */

	/* Letra a */
	bool a = (r+5)>t;
	printf("%d\n", a);

	/* Letra b */
	// (...)

	/* Letra c */
	bool c = (-r == s) && (s > r);
	printf("%d\n", c);

	return 0;
}
